# this file is only useful for local deployment
# the actual deployment is done by github pages, and they use their own Gemfile

source "https://rubygems.org"

# this line puts the local environment in the same state as github pages
gem "github-pages", group: :jekyll_plugins
